# Backend Configuration
# Use this for production deployments where frontend and backend are on different domains
# In development with vite dev server, the proxy will handle /api requests automatically

# Backend API base URL (used in production)
# Example: https://your-backend-domain.com/api
VITE_API_BASE_URL=

# Alternative: Backend root URL (the /api will be appended automatically)
# Example: https://your-backend-domain.com
VITE_BACKEND_URL=https://jarvis-production-5709a.up.railway.app

# Development mode (set by vite automatically)
# VITE_DEV=true
