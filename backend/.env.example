# Jarvis Backend Environment Configuration
# Copy this file to .env and fill in the values

# OpenAI Configuration (Optional - system works without it)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=True

# Frontend URL (for CORS in production)
FRONTEND_URL=http://localhost:5173

# Railway Configuration (for deployment)
# RAILWAY_ENVIRONMENT_NAME will be set automatically when deployed to Railway
